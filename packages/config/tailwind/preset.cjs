// Shared Tailwind preset. Design tokens land here via issue #12 - until then
// this is intentionally close to empty so nobody builds on colours/spacing
// that are about to be replaced.
/** @type {import('tailwindcss').Config} */
module.exports = {
  theme: {
    extend: {},
  },
  plugins: [],
};
